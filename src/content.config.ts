// Projects are authored as typed TypeScript modules under
// src/content/projects/*.ts and imported directly by ProjectSection.
// We export an empty collections map so Astro doesn't auto-generate
// a markdown-shaped collection for that folder.
export const collections = {};
